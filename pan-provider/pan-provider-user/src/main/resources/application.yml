spring:
  application:
    name: pan-provider-user
  profiles:
    active: dev
pagehelper:
    helperDialect: mysql
    reasonable: true
    supportMethodsArguments: true
    params: count=countSql